const Navbar = () => {
    return (
        <nav>
            <h1 className="sitetitle">STUDYKIT</h1>
            <div className="links">
                <a href="#">Home</a>
                <a href="#">New Test</a>
                <a href="#">Profile</a>
            </div>
        </nav>
    );
}

export default Navbar;